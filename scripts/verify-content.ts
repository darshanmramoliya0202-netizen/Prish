/**
 * Content gate — runs in `prebuild` and CI.
 * Phase 1 fills this with the zod schema, forbidden-string sweep and asset checks.
 */
console.log("verify:content — no content model yet (phase 0)");
