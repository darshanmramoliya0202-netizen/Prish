/**
 * Build-time PDF generation (spec sheets + catalogue) via @react-pdf/renderer.
 * Phase 5 implements this; the stub keeps `prebuild` green until then.
 */
console.log("build:pdfs — nothing to render yet (phase 0)");
