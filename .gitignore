# dependencies
node_modules/
# next
.next/
out/
next-env.d.ts
*.tsbuildinfo
# env
.env
.env.*
!.env.example
# logs / local data
logs/
data/leads/
# generated at build (VM) — never committed
public/downloads/
*.pdf
# os / editors
.DS_Store
Thumbs.db
.vscode/
.idea/
# archives of the old site live on F:, never here
Web V2/
Reference/
/photos/
brochure/
# photo originals stay on F: (owner drops them here, runs photos:prep, commits public/photos)
assets-src/photos/
# playwright / lighthouse
test-results/
playwright-report/
.lighthouseci/
public/illustrations/products/.manifest.json
